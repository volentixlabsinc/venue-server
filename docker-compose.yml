version: '3'
services:
    web:
        image: volentixlabs/venueserver:v0.0.15-beta
        build: .
        restart: 'always'
        expose:
            - '8000'
            - '9000'
            - '5555'
        ports:
            - '8000:8000'
            - '9000:9000'
            - '5555:5555'
        volumes:
            - '.:/code'
        stdin_open: true
        tty: true
        entrypoint: '/entrypoint.sh'
        command: supervisord -c ./supervisord.conf --nodaemon
    redis:
        image: 'bitnami/redis:latest'
        expose:
            - '6379'
        environment:
            REDIS_PASSWORD: '4e7a84d5'
    postgres:
        image: 'postgres'
        expose:
            - '5432'
        environment:
            POSTGRES_DB: 'venuepostgress'
            POSTGRES_USER: 'venueadmin'
            POSTGRES_PASSWORD : 'BxKkpaihl67B'
